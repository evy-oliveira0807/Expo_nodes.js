import styled from "styled-components";

export const ContainerBoxAlign = styled.View`
    height: 100%;
    width: 87%;
    align-items: center;
    /* Short line tip: first number(Top and botton) second number(Left and right) */
    margin: 0px 22px;
`

export const BoxContent = styled.View`
    width: 100%;
`

export const BoxInput = styled.View`
    flex-direction: row;
    width: 100%;
    justify-content: space-between;
    margin-top: 10px;
`